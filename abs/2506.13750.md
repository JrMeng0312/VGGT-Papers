### Test3R: Learning to Reconstruct 3D at Test Time

Dense matching methods like DUSt3R regress pairwise pointmaps for 3D reconstruction. However, the reliance on pairwise prediction and the limited generalization capability inherently restrict the global geometric consistency. In this work, we introduce Test3R, a surprisingly simple test-time learning technique that significantly boosts geometric accuracy. Using image triplets, Test3R generates reconstructions from pairs and optimizes the network at test time via a self-supervised objective: maximizing the geometric consistency between these two reconstructions relative to the common image. This ensures the model produces cross-pair consistent outputs, regardless of the inputs. Extensive experiments demonstrate that our technique significantly outperforms previous state-of-the-art methods on the 3D reconstruction and multi-view depth estimation tasks. Moreover, it is universally applicable and nearly cost-free, making it easily applied to other models and implemented with minimal test-time training overhead and parameter footprint.

DUSt3R 等密集匹配方法通过回归成对点图实现三维重建。然而，依赖成对预测和有限的泛化能力固有地限制了全局几何一致性。在本文中，我们提出了 Test3R，一种简单而有效的测试时学习技术，可以显著提升几何精度。Test3R 使用图像三元组从图像对生成重建，并在测试时通过自监督目标优化网络：最大化两个重建相对于公共图像的几何一致性。这确保模型产生跨图像对一致的输出，无论输入如何。大量实验表明，我们的技术在三维重建和多视图深度估计任务上显著优于先前最先进的方法。此外，它具有通用适用性且几乎零成本，可以轻松应用于其他模型，测试时训练开销和参数占用极小。
